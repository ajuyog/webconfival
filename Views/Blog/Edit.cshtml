@model frontend.Models.BlogsDTO
@{
    Layout = "_Layout3";
}
<!-- Miga de pan -->
<div class="page-header">
	<div>
		<h1 class="page-title">Podras: editar contenido de los Blogs y aprobar los comentarios</h1>
	</div>
	<div class="ms-auto pageheader-btn">
		<ol class="breadcrumb">
			<li class="breadcrumb-item">Publicaciones</li>
			<li class="breadcrumb-item active" aria-current="page">Blogs</li>
			<li class="breadcrumb-item active" aria-current="page">Editar</li>
		</ol>
	</div>
</div>

<div class="row">
	<div class="col-xl-12 col-md-12">
		<div class="card cart">
			<div class="card-header border-bottom">
				<h3 class="card-title">Listado de Blogs</h3>
			</div>
			<div class="card-body">
				<div class="table-responsive">
					<table class="table table-bordered border-bottom text-nowrap">
						<thead>
							<tr class="border-top">
								<th class="w-15">Id</th>
								<th class="w-5">Nombre</th>
								<th class="w-5">Estado</th>
								<th class="w-5">Comentarios aprobados</th>
								<th class="w-5">Fecha publicado</th>
								<th class="w-5">Categoria princial</th>
								<th class="w-5">Action</th>
							</tr>
						</thead>
						<tbody>
							@foreach (var item in Model.ResultBlog)
							{
								<tr>
									<td>
										<span class="badge bg-primary">@item.Id</span>
									</td>
									<td>
										@item.Titulo
									</td>
									<td>
										@(item.Estado == true ? "Activo" : "No aprovado")
									</td>
									<td>
										Total: @item.Comentarios.Count()
									</td>
									<td>
										@item.Publicacion.ToShortDateString();
									</td>
									<td>
										@(item.Categorias == null ? "" : item.Categorias[0].Nombre)
									</td>
									<td>
										<a asp-action="EditBlog" asp-route-id="@item.Id" class="btn btn-primary-light btn-square  br-50 m-1" data-bs-toggle="tooltip" title="" data-bs-original-title="Editar Contenido"><i class="fe fe-edit"></i></a>
										<a asp-controller="Comentarios" asp-action="EditComment" asp-route-idBlog="@item.Id" asp-route-titulo="@item.Titulo" class="btn btn-success-light btn-square  br-50 m-1" data-bs-toggle="tooltip" title="" data-bs-original-title="Aprobar Comentarios"><i class="fe fe-message-square"></i></a>
									</td>
								</tr>
							}
						</tbody>
					</table>
				</div>

			</div>
		</div>
		<partial name="~/Views/Shared/_Paginador.cshtml" />
	</div>
</div>
<!-- ROW-1 CLOSED -->


